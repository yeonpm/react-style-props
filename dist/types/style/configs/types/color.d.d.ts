type ColorConfigType = {
  background?: string;
  backgroundColor?: string;
  /** Equals to background. */
  bg?: string;
  color?: string;
  white?: boolean;
  red?: boolean;
};

export type { ColorConfigType };
