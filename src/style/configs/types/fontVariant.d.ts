export interface FontVariant {
  fontSize?: numStr;
  fontWeight?: numStr;
  lineHeight?: numStr;
  letterSpacing?: numStr;
  fontFamily?: string;
  fontStyle?: string;

  fs?: numStr;
  fw?: numStr;
  lh?: numStr;
  ls?: numStr;
  ff?: string;
  fst?: string;
}
